[project]
name = "ha-lobehub"
version = "0.2.0"
description = "Home Assistant custom integration for LobeHub"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
test = [
    "pytest>=8.3,<9",
    "pytest-cov>=6.0,<7",
    "voluptuous>=0.15",
]
lint = ["ruff>=0.12"]
dev = [
    { include-group = "lint" },
    { include-group = "test" },
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["custom_components*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --cov=custom_components.lobehub --cov-report=term-missing --cov-report=xml --cov-fail-under=90"

[tool.coverage.run]
branch = true
source = ["custom_components/lobehub"]

[tool.coverage.report]
show_missing = true
skip_covered = false

[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
